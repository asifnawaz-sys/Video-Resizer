name: Build VideoResizer macOS App

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-macos-app:
    runs-on: macos-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install all dependencies
        run: |
          pip install --upgrade pip
          pip install pyinstaller
          pip install imageio-ffmpeg
          pip install moviepy
          pip install opencv-python
          pip install Pillow
          pip install ttkbootstrap
          pip install ultralytics
          pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu

      - name: Verify imageio-ffmpeg works
        run: |
          python3 -c "
          import imageio_ffmpeg
          ffmpeg = imageio_ffmpeg.get_ffmpeg_exe()
          print(f'ffmpeg path: {ffmpeg}')
          import os
          print(f'ffmpeg exists: {os.path.exists(ffmpeg)}')
          import subprocess
          r = subprocess.run([ffmpeg, '-version'], capture_output=True, text=True)
          print(r.stdout.splitlines()[0])
          print('imageio-ffmpeg OK!')
          "

      - name: Build macOS .app with PyInstaller
        run: |
          pyinstaller \
            --windowed \
            --onedir \
            --name "VideoResizer" \
            --osx-bundle-identifier "com.siardigital.videoresizer" \
            --hidden-import cv2 \
            --hidden-import PIL \
            --hidden-import PIL.Image \
            --hidden-import PIL.ImageTk \
            --hidden-import PIL.ImageFilter \
            --hidden-import ttkbootstrap \
            --hidden-import ttkbootstrap.themes \
            --hidden-import ttkbootstrap.style \
            --hidden-import ultralytics \
            --hidden-import ultralytics.models \
            --hidden-import ultralytics.models.yolo \
            --hidden-import ultralytics.nn \
            --hidden-import ultralytics.utils \
            --hidden-import moviepy \
            --hidden-import moviepy.editor \
            --hidden-import moviepy.video \
            --hidden-import moviepy.audio \
            --hidden-import tkinter \
            --hidden-import tkinter.ttk \
            --hidden-import tkinter.filedialog \
            --hidden-import tkinter.messagebox \
            --hidden-import torch \
            --hidden-import torchvision \
            --hidden-import imageio_ffmpeg \
            --collect-all ultralytics \
            --collect-all ttkbootstrap \
            --collect-all moviepy \
            --collect-all cv2 \
            --collect-all imageio_ffmpeg \
            --noconfirm \
            --clean \
            video_resizer_asif.py

      - name: Verify ffmpeg is inside .app
        run: |
          find dist/VideoResizer.app -name "ffmpeg*" -type f 2>/dev/null || echo "searching..."
          python3 -c "
          import sys
          sys.path.insert(0, 'dist/VideoResizer.app/Contents/MacOS/_internal')
          import imageio_ffmpeg
          ffmpeg = imageio_ffmpeg.get_ffmpeg_exe()
          print(f'ffmpeg inside app: {ffmpeg}')
          import os
          print(f'exists: {os.path.exists(ffmpeg)}')
          " 2>/dev/null || echo "verification skipped"

      - name: Zip the .app for download
        run: |
          cd dist
          zip -r ../VideoResizer_macOS_SiarDigital_v2.zip VideoResizer.app
          cd ..
          echo "Final zip size:"
          du -sh VideoResizer_macOS_SiarDigital_v2.zip

      - name: Upload .app as artifact
        uses: actions/upload-artifact@v4
        with:
          name: VideoResizer-macOS-v2
          path: VideoResizer_macOS_SiarDigital_v2.zip
          retention-days: 30

