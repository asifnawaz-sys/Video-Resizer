name: Build ImageResizer macOS App

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-macos-app:
    runs-on: macos-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Python dependencies
        run: |
          pip install --upgrade pip
          pip install pyinstaller
          pip install customtkinter
          pip install Pillow
          pip install opencv-python
          pip install numpy
          pip install ultralytics
          pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
          pip install onnxruntime
          pip install "rembg[cpu]"
          pip install mediapipe
          pip install openpyxl
          pip install psutil

      - name: Build macOS .app with PyInstaller
        run: |
          pyinstaller \
            --windowed \
            --onedir \
            --name "ImageResizer" \
            --osx-bundle-identifier "com.siardigital.imageresizer" \
            --hidden-import customtkinter \
            --hidden-import PIL \
            --hidden-import PIL.Image \
            --hidden-import PIL.ImageTk \
            --hidden-import PIL.ImageOps \
            --hidden-import PIL.ImageFilter \
            --hidden-import PIL.ImageDraw \
            --hidden-import PIL.ImageEnhance \
            --hidden-import tkinter \
            --hidden-import tkinter.ttk \
            --hidden-import tkinter.filedialog \
            --hidden-import tkinter.messagebox \
            --hidden-import cv2 \
            --hidden-import numpy \
            --hidden-import ultralytics \
            --hidden-import ultralytics.models \
            --hidden-import ultralytics.models.yolo \
            --hidden-import ultralytics.nn \
            --hidden-import ultralytics.utils \
            --hidden-import torch \
            --hidden-import torchvision \
            --hidden-import onnxruntime \
            --hidden-import rembg \
            --hidden-import mediapipe \
            --hidden-import openpyxl \
            --hidden-import openpyxl.styles \
            --hidden-import psutil \
            --collect-all customtkinter \
            --collect-all ultralytics \
            --collect-all cv2 \
            --collect-all mediapipe \
            --collect-all rembg \
            --collect-all onnxruntime \
            --noconfirm \
            --clean \
            image_resizer_asif.py

      - name: Zip the .app for download
        run: |
          cd dist
          zip -r ../ImageResizer_macOS_SiarDigital.zip ImageResizer.app
          cd ..
          echo "Final zip size:"
          du -sh ImageResizer_macOS_SiarDigital.zip

      - name: Upload .app as artifact
        uses: actions/upload-artifact@v4
        with:
          name: ImageResizer-macOS
          path: ImageResizer_macOS_SiarDigital.zip
          retention-days: 30

